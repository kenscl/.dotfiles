{
  "layer": "top",
  "position": "top",

  "modules-left": [
    "clock",
    "hyprland/workspaces",
  ],

  "modules-center": [
    "hyprland/window",
  ],

  "modules-right": [
    "cpu",
    "gpu",
    "memory",
    "battery",
  ],

  "clock": {
    "format": "time: {:%H:%M}"
  },

  "battery": {
    "format": "[battery: {capacity}%]",
    "format-charging": "[battery: {capacity}%]"
  },

  "cpu": {
    "format": "[cpu: {usage}%]"
  },

  "memory": {
    "format": "[mem: {used:0.1f}G]"
  },

  "gpu": {
    "format": "[gpu: {usage}%]"
  },

  "hyprland/window": {
    "format": "win: {title}",
    "max-length": 50
  },

  "hyprland/workspaces": {
    "format": "{icon}"
  }
}
